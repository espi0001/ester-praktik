---
import { Icon } from "astro-icon/components";
---

<footer>
  <div class="content">
    <div class="logo">
      <a href="/"> <Icon name="LogoWhite" /></a>
    </div>
    <div class="sociale_links">
      <a href="https://www.linkedin.com/in/ester-piazza-koldbye-3698181b9/" target="_blank"> <Icon name="linked" /></a>
      <a href="https://github.com/espi0001" target="_blank"> <Icon name="github" /></a>
      <a href="mailto:ester.piazza.koldbye@gmail.com?" target="_blank"> <Icon name="mail" /></a>
      <a href="tel:+4528923400" target="_blank"> <Icon name="phone" /></a>
    </div>

    <hr />

    <p>&copy; 2024 Ester Piazza-Koldbye</p>
  </div>
</footer>

<style>
  @layer components {
    footer {
      align-items: center;
      grid-column: content;

      display: grid;
      grid-template-columns: var(--layout-grid);
      padding: var(--space-s-m);

      background: var(--baggrund);
      color: var(--sort);
    }

    .content {
      display: flex;
      grid-column: content;
      flex-direction: column;
      align-items: center;
      gap: var(--space-s-m);

      hr {
        width: 100%;
        color: var(--sort);
      }
    }
    .logo {
      max-width: 55px;
      height: auto;

      a {
        color: var(--sort);
      }
    }
    .sociale_links {
      display: flex;
      gap: 12px;
      align-items: center;
      justify-content: center;
      a {
        background-color: var(--baggrund);
        border-radius: 50px;
        width: 40px;
        height: auto;

        svg {
          transition: all 0.4s ease-in-out;
          &:hover {
            color: var(--brun);
          }
        }
      }
    }

    p {
      color: var(--sort);
    }
  }
</style>
